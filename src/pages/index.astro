---
import SiteLayout from "../layouts/SiteLayout.astro";
import { Image } from "astro:assets";
import { HOMEPAGE_OPERATIONAL_GRIP_EXPLANATION, pageSeo } from "../seo/registry.mjs";
import heroBoundaryVisual from "../assets/homepage/team-boundaries-diffuse.svg";
import heroBoundaryHoverVisual from "../assets/homepage/homepage-hero-hover-valued-result.svg";
import talesCardFragment from "../assets/doomscroll/tales-card-fragment-muted.jpg";
import bowtieCard from "../assets/papers/bowtie-card-muted.jpg";

const diagnostics = [
	{
		number: "01",
		title: "One person holds the tapestry",
		body: "Too much authority, context, and sequencing dependency has collapsed into one decision-maker.",
	},
	{
		number: "02",
		title: "Local work stops staying local",
		body: "A small change creates unpredictable dependencies, rework, or downstream damage.",
	},
	{
		number: "03",
		title: "Coordination becomes reporting",
		body: "Meetings describe activity but do not route the cross-boundary work teams cannot safely resolve alone.",
	},
	{
		number: "04",
		title: "Demand is silenced",
		body: "Bloated backlogs, status theater, delayed feedback, and excessive process weaken the signal from the work.",
	},
];

const loop = ["Demand", "Organizational response", "Delivered value", "Market acceptance", "Revenue"];

const approach = [
	{
		title: "Observe the condition",
		body: "Follow real demand, decisions, work, dependencies, feedback, and downstream effects.",
	},
	{
		title: "Bound the intervention",
		body: "Choose one problem connected to real demand, safe enough to change, and visible enough to learn from.",
	},
	{
		title: "Place decision ownership",
		body: "Give one accountable role the judgment needed to act within a clear authority boundary.",
	},
	{
		title: "Inspect the result",
		body: "Observe what changed, what propagated, and what the organization can now see well enough to do next.",
	},
];

const assessmentResults = [
	{
		key: "demand",
		label: "Response to Changing Demand",
		score: 95,
		strength: "Strong",
	},
	{
		key: "ownership",
		label: "Ownership and Bounded Execution",
		score: 35,
		strength: "Weak",
	},
	{
		key: "results",
		label: "Observable and Traceable Results",
		score: 90,
		strength: "Strong",
	},
	{
		key: "adaptation",
		label: "Adaptation Without Loss of Control",
		score: 90,
		strength: "Strong",
	},
	{
		key: "ai",
		label: "AI Use and Control",
		score: 60,
		strength: "Moderate",
	},
];

const overallGripScore = Math.round(
	assessmentResults.reduce((total, result) => total + result.score, 0) / assessmentResults.length,
);
---

<SiteLayout
	metadata={pageSeo["/"]}
>
	<main id="main">
			<section class="hero section-rule">
				<div class="shell hero-grid">
					<div class="hero-copy">
						<p class="eyebrow">3Back <span aria-hidden="true">|</span> The Team Execution Company</p>
						<h1>Organizations do not outgrow teams.<br /><em>They lose the boundaries that made teams effective.</em></h1>
						<p class="hero-intro">
							When decisions, dependencies, and coordination spread without control, work slows,
							accountability diffuses, and demand gets lost. 3Back helps leaders find where execution has
							lost grip and make bounded changes that reveal what to do next.
						</p>
						<div class="button-row">
							<a class="button" href="/contact/">Start a Conversation</a>
							<a class="text-link" href="/operational-grip/">Explore Operational Grip <span aria-hidden="true">&#8594;</span></a>
						</div>
					</div>

					<div class="hero-aside hero-boundary-control" tabindex="0" role="button" aria-pressed="false" aria-label="Show the valued-result view of the team boundary diagram" data-hero-diagram>
						<Image class="hero-boundary-visual hero-boundary-visual-default" src={heroBoundaryVisual} alt="" />
						<Image class="hero-boundary-visual hero-boundary-visual-hover" src={heroBoundaryHoverVisual} alt="" />
					</div>
				</div>
			</section>

			<section class="section section-rule" data-reveal aria-labelledby="loss-heading">
				<div class="shell">
					<div class="section-heading split-heading">
						<div>
							<p class="eyebrow">Observable problem</p>
							<h2 id="loss-heading">An organization can look busy while losing control of the work.</h2>
						</div>
						<p class="lead-copy">The problem is rarely a lack of activity. It is the growing distance between demand, decisions, work, and observable results.</p>
					</div>

					<ol class="diagnostic-grid">
						{diagnostics.map((item) => (
							<li tabindex="0" role="button" aria-pressed="false" data-diagnostic-card>
								<div class="diagnostic-number-row">
									<span class="item-number">{item.number}</span>
									<span class="diagnostic-ticks" aria-hidden="true">
										{Array.from({ length: Number(item.number) }).map(() => <i></i>)}
									</span>
								</div>
								<h3>{item.title}</h3>
								<p>{item.body}</p>
							</li>
						))}
					</ol>

					<p class="closing-line">These are not isolated process problems. They are evidence that the organization may be losing <strong>Operational Grip.</strong></p>
				</div>
			</section>

			<section class="grip-section" id="operational-grip" data-reveal aria-labelledby="grip-heading">
				<div class="shell grip-grid">
					<div class="grip-copy">
						<p class="eyebrow eyebrow-light">Operational Grip</p>
						<h2 id="grip-heading">Can the people closest to the work make a local change whose effects stay observable and correctable?</h2>
						<div class="grip-body">
							<p class="operational-grip-explanation">{HOMEPAGE_OPERATIONAL_GRIP_EXPLANATION}</p>
							<p>It is not total system understanding. It is enough actionable control to adapt intelligently while keeping work connected to purpose, quality, and value.</p>
						</div>
						<p class="operating-line">Bound what you can. Act locally. Observe the effects. Learn.</p>
					</div>

					<div class="grip-diagram" tabindex="0" role="button" aria-pressed="false" aria-label="A local action inside an observable boundary creates downstream effects">
						<p class="diagram-question diagram-question-top">What am I missing?</p>
						<div class="diagram-stage">
							<div class="local-boundary">
								<span class="diagram-label">Observable boundary</span>
								<div class="local-action">Local<br />action</div>
							</div>
							<div class="effect-line" aria-hidden="true"><span></span><span></span><span></span></div>
							<div class="downstream">Downstream<br />effects</div>
						</div>
						<p class="diagram-question diagram-question-bottom">How would I know?</p>
					</div>
				</div>
			</section>

			<section class="section loop-section" data-reveal aria-labelledby="loop-heading">
				<div class="shell">
					<div class="section-heading split-heading">
						<div>
							<p class="eyebrow">The commercial execution loop</p>
							<h2 id="loop-heading">Work matters only when the organization can connect demand to market value.</h2>
						</div>
						<p class="lead-copy">3Back examines the commercial execution loop. The question is where the signal from demand weakens, ownership diffuses, and feedback arrives too late to guide action.</p>
					</div>

					<ol class="loop-flow">
						{loop.map((item, index) => (
							<li class:list={[index === 0 && "signal-step"]} tabindex="0" role="button" aria-pressed="false" data-loop-stage>
								{index === 1 && <span class="loop-group-badge loop-group-badge-organization" aria-hidden="true">Organization</span>}
								{index === 3 && <span class="loop-group-badge loop-group-badge-market" aria-hidden="true">Market</span>}
								<span>{String(index + 1).padStart(2, "0")}</span>
								<strong>{item}</strong>
							</li>
						))}
					</ol>

					<p class="loop-note">Revenue is evidence the market accepted the organization&rsquo;s response to demand. The work is to restore enough grip to see, act, and learn before demand disappears into activity and revenue is lost.</p>
				</div>
			</section>

			<section class="assessment-section section-rule" id="grip-assessment" aria-labelledby="assessment-heading">
				<div class="shell assessment-grid">
					<div class="assessment-copy">
						<p class="eyebrow eyebrow-light">Operational Grip assessment</p>
						<h2 id="assessment-heading">How strong is your organization's grip?</h2>
						<p class="assessment-intro">See where execution is holding firm and where it is losing grip.</p>
						<ul class="assessment-commitments" aria-label="Assessment details">
							<li>20 questions</li>
							<li>About 7 minutes</li>
							<li>Immediate results</li>
						</ul>
						<a class="button assessment-button" href="/grip-check/">Check Your Grip</a>
					</div>

					<article class="score-card" id="grip-score-card" aria-labelledby="score-card-heading">
						<p class="score-eyebrow" id="score-card-heading">Sample Grip Score</p>
						<div class="score-profile">
							<svg class="score-ring" viewBox="0 0 520 300" role="group" aria-label="Five-section Operational Grip score profile">
								<g transform="translate(260 250)">
									<path id="ring-demand" data-section="demand" class="score-segment score-segment-strong" d="M -210 0 A 210 210 0 0 1 -170 -123" role="button" tabindex="0" aria-controls="result-demand" aria-label="Response to Changing Demand, 95 percent, Strong" aria-pressed="false"></path>
									<path id="ring-ownership" data-section="ownership" class="score-segment score-segment-weak" d="M -158 -139 A 210 210 0 0 1 -65 -200" role="button" tabindex="0" aria-controls="result-ownership" aria-label="Ownership and Bounded Execution, 35 percent, Weak" aria-pressed="false"></path>
									<path id="ring-results" data-section="results" class="score-segment score-segment-strong" d="M -48 -204 A 210 210 0 0 1 48 -204" role="button" tabindex="0" aria-controls="result-results" aria-label="Observable and Traceable Results, 90 percent, Strong" aria-pressed="false"></path>
									<path id="ring-adaptation" data-section="adaptation" class="score-segment score-segment-strong" d="M 65 -200 A 210 210 0 0 1 158 -139" role="button" tabindex="0" aria-controls="result-adaptation" aria-label="Adaptation Without Loss of Control, 90 percent, Strong" aria-pressed="false"></path>
									<path id="ring-ai" data-section="ai" class="score-segment score-segment-moderate" d="M 170 -123 A 210 210 0 0 1 210 0" role="button" tabindex="0" aria-controls="result-ai" aria-label="AI Use and Control, 60 percent, Moderate" aria-pressed="false"></path>
								</g>
							</svg>
							<p class="overall-score" aria-label={`Overall score ${overallGripScore} out of 100`}>
								<strong>{overallGripScore}</strong><span>/100</span>
							</p>
							<img class="hands-bar" src="/assets/operational-grip/hands-bar.png" alt="One hand holds a bar securely while the other begins to lose its grip." />
						</div>

						<div class="score-results" aria-label="Sample results by assessment section">
							{assessmentResults.map((result) => (
								<button
									class:list={["score-result", `score-result-${result.key}`]}
									id={`result-${result.key}`}
									type="button"
									data-section={result.key}
									aria-controls={`ring-${result.key}`}
									aria-pressed="false"
								>
									<span class="score-icon" aria-hidden="true"></span>
									<span class="result-label">{result.label}</span>
									<strong>{result.score}%</strong>
									<span class="result-strength">{result.strength}</span>
									<span class="mobile-grip-summary" aria-hidden="true">
										<span>{result.score}%</span>
										<img src="/assets/operational-grip/hands-bar.png" alt="" />
									</span>
								</button>
							))}
						</div>

						<div class="overall-reading">
							<h3>Overall Reading</h3>
							<p>Generally strong grip with a concentrated weakness in Ownership and Bounded Execution.</p>
						</div>
					</article>
				</div>
			</section>

			<section class="section approach-section section-rule" data-reveal aria-labelledby="approach-heading">
				<div class="shell">
					<div class="section-heading narrow-heading">
						<p class="eyebrow">How 3Back works</p>
						<h2 id="approach-heading">Evidence first. One bounded problem at a time.</h2>
						<p class="lead-copy">3Back examines the actual work system before recommending a change. We preserve what works, identify the real constraint, and recommend a change that can be owned, observed, and corrected.</p>
					</div>

					<ol class="approach-list">
						{approach.map((item, index) => (
							<li tabindex="0" role="button" aria-pressed="false" data-approach-step>
								<span class="approach-number">{String(index + 1).padStart(2, "0")}</span>
								<div>
									<h3>{item.title}</h3>
									<p>{item.body}</p>
								</div>
								<span class="mobile-approach-one" aria-hidden="true">one</span>
							</li>
						))}
					</ol>

					<blockquote class="standard-quote"><span class="approach-one approach-one-1">One</span> bounded problem, <span class="approach-one approach-one-2">one</span> decision, <span class="approach-one approach-one-3">one</span> accountable owner, and <span class="approach-one approach-one-4">one</span> observable result at a time.</blockquote>
				</div>
			</section>

			<section class="section body-work-section" aria-labelledby="body-work-heading">
				<div class="shell body-work-grid">
					<div class="body-work-lead">
						<p class="eyebrow">A body of work built around execution</p>
						<h2 id="body-work-heading">Operational Grip is the current operating lens guiding 3Back&rsquo;s work.</h2>
						<p>3Back has spent years examining how teams form, make decisions, adapt, coordinate, and deliver value. Operational Grip is the culmination of that work and the primary lens guiding 3Back now.</p>
					</div>

					<div class="body-work-links">
						<a href="/ideas/">
							<span>01</span>
							<h3>Well-Formed Teams</h3>
							<p>The team and authority structure beneath accountable execution.</p>
						</a>
						<a href="/ideas/">
							<span>02</span>
							<h3>Decision ownership and adaptation</h3>
							<p>How authority, local control, and feedback shape the ability to respond to changing demand.</p>
						</a>
						<a href="/ideas/">
							<span>03</span>
							<h3>Agility, Scrum, publications, and learning</h3>
							<p>A continuing body of work that informs how 3Back thinks about teams, execution, and adaptation.</p>
						</a>
					</div>
				</div>
				<div class="shell boundary-statement">3Back is the enduring brand. Operational Grip leads the current argument. No single concept independently defines the company.</div>
			</section>

			<section class="section thinking-section section-rule" aria-labelledby="thinking-heading">
				<div class="shell">
					<div class="section-heading split-heading">
						<div>
							<p class="eyebrow">Current thinking</p>
							<h2 id="thinking-heading">Ideas for seeing the system before changing it</h2>
						</div>
						<p class="lead-copy">Examine the mechanism. Find the evidence. Then decide where action can reveal more of the system.</p>
					</div>

					<div class="thinking-grid">
						<article class="paper-card">
							<a class="paper-card-link" href="/papers/no-head-works-alone/">
								<p class="content-type">Paper</p>
								<h3 class="paper-title">No Head Works Alone</h3>
								<div class="paper-fragment" aria-hidden="true"><Image src={bowtieCard} alt="" widths={[220, 320, 440]} sizes="(max-width: 720px) min(100%, 320px), 220px" loading="lazy" /></div>
								<p class="paper-line">When the system works only because one head holds the whole context, that person may have control. The organization does not.</p>
							</a>
						</article>
						<article class="tales-card">
							<a class="tales-card-link" href="/doomscroll/">
								<p class="content-type">Doomscroll</p>
								<h3 class="tales-title">Tales of the Grip</h3>
								<div class="tales-fragment" aria-hidden="true">
									<Image src={talesCardFragment} alt="" widths={[160, 240, 320]} sizes="(max-width: 720px) 160px, 240px" loading="lazy" />
								</div>
								<p class="tales-line">The decision is local. The permission structure is not.</p>
								<p class="tales-coming">Coming soon</p>
							</a>
						</article>
					</div>
				</div>
			</section>

			<section class="contact-section" id="contact" aria-labelledby="contact-heading">
				<div class="shell contact-grid">
					<div>
						<p class="eyebrow eyebrow-light">Start with the work in front of you</p>
						<h2 id="contact-heading">Find one place where action will reveal the system.</h2>
					</div>
					<div class="contact-copy">
						<p>You do not need the perfect place to start. Bring one execution problem where demand is real, ownership is unclear, or local action keeps creating downstream effects. We will examine whether it is bounded enough to act on and observable enough to learn from.</p>
						<a class="button" href="/contact/" aria-describedby="contact-note">Start a Conversation</a>
						<p class="contact-note" id="contact-note">No transformation pitch. No promise of a universal answer. Start with the work in front of you.</p>
					</div>
				</div>
			</section>
	</main>

		<script>
			const clearOnExit = (section: HTMLElement, clear: () => void) => {
				const observer = new IntersectionObserver(([entry]) => {
					if (!entry.isIntersecting) clear();
				}, { threshold: 0.1 });
				observer.observe(section);
			};

			const togglePressed = (controls: HTMLElement[], active: HTMLElement | null, activeClass = "is-active") => {
				controls.forEach((control) => {
					const isActive = control === active;
					control.classList.toggle(activeClass, isActive);
					control.setAttribute("aria-pressed", String(isActive));
				});
			};

			const bindTapParity = (section: HTMLElement | null, selector: string, onActive: (active: HTMLElement | null) => void) => {
				if (!section) return;
				const controls = Array.from(section.querySelectorAll<HTMLElement>(selector));
				let selected: HTMLElement | null = null;
				const setSelected = (control: HTMLElement | null) => {
					selected = control;
					onActive(selected);
				};

				controls.forEach((control) => {
					control.addEventListener("pointerenter", (event) => {
						if ((event as PointerEvent).pointerType !== "touch") onActive(control);
					});
					control.addEventListener("pointerleave", (event) => {
						if ((event as PointerEvent).pointerType !== "touch") onActive(selected);
					});
					control.addEventListener("focus", () => onActive(control));
					control.addEventListener("blur", () => onActive(selected));
					control.addEventListener("click", (event) => {
						event.stopPropagation();
						setSelected(selected === control ? null : control);
					});
					control.addEventListener("keydown", (event) => {
						if (event.key === " " || event.key === "Enter") {
							event.preventDefault();
							setSelected(selected === control ? null : control);
						}
					});
				});

				document.addEventListener("click", () => setSelected(null));
				clearOnExit(section, () => setSelected(null));
			};

			const diagnosticSection = document.querySelector<HTMLElement>(".diagnostic-grid")?.closest<HTMLElement>("section");
			bindTapParity(diagnosticSection ?? null, "[data-diagnostic-card]", (active) => {
				if (!diagnosticSection) return;
				togglePressed(Array.from(diagnosticSection.querySelectorAll<HTMLElement>("[data-diagnostic-card]")), active);
				diagnosticSection.classList.toggle("has-active-card", active !== null);
			});

			const heroDiagram = document.querySelector<HTMLElement>("[data-hero-diagram]");
			if (heroDiagram) {
				let heroDiagramSelected = false;
				const setHeroDiagramSelected = (active: boolean) => {
					heroDiagramSelected = active;
					heroDiagram.classList.toggle("is-active", active);
					heroDiagram.setAttribute("aria-pressed", String(active));
				};
				heroDiagram.addEventListener("click", (event) => {
					event.stopPropagation();
					setHeroDiagramSelected(!heroDiagramSelected);
				});
				heroDiagram.addEventListener("keydown", (event) => {
					if (event.key === " " || event.key === "Enter") {
						event.preventDefault();
						setHeroDiagramSelected(!heroDiagramSelected);
					}
				});
				document.addEventListener("click", () => setHeroDiagramSelected(false));
				clearOnExit(heroDiagram.closest<HTMLElement>("section")!, () => setHeroDiagramSelected(false));
			}

			const gripDiagram = document.querySelector<HTMLElement>(".grip-diagram");
			if (gripDiagram) {
				let diagramSelected = false;
				const applyDiagramState = (active: boolean) => {
					gripDiagram.classList.toggle("is-active", active);
					gripDiagram.setAttribute("aria-pressed", String(active));
				};
				const setDiagramSelected = (active: boolean) => {
					diagramSelected = active;
					applyDiagramState(active);
				};
				gripDiagram.addEventListener("pointerenter", (event) => {
					if ((event as PointerEvent).pointerType !== "touch") applyDiagramState(true);
				});
				gripDiagram.addEventListener("pointerleave", (event) => {
					if ((event as PointerEvent).pointerType !== "touch") applyDiagramState(diagramSelected);
				});
				gripDiagram.addEventListener("focus", () => applyDiagramState(true));
				gripDiagram.addEventListener("blur", () => applyDiagramState(diagramSelected));
				gripDiagram.addEventListener("click", (event) => {
					event.stopPropagation();
					setDiagramSelected(!diagramSelected);
				});
				gripDiagram.addEventListener("keydown", (event) => {
					if (event.key === " " || event.key === "Enter") {
						event.preventDefault();
						setDiagramSelected(!diagramSelected);
					}
				});
				document.addEventListener("click", () => setDiagramSelected(false));
				clearOnExit(gripDiagram.closest<HTMLElement>("section")!, () => setDiagramSelected(false));
			}

			const loopSection = document.querySelector<HTMLElement>(".loop-flow")?.closest<HTMLElement>("section");
			bindTapParity(loopSection ?? null, "[data-loop-stage]", (active) => {
				if (!loopSection) return;
				const stages = Array.from(loopSection.querySelectorAll<HTMLElement>("[data-loop-stage]"));
				const index = active ? stages.indexOf(active) : -1;
				const group = index < 0 ? "" : index < 3 ? "organization" : "market";
				loopSection.dataset.activeGroup = group;
				stages.forEach((stage, stageIndex) => {
					const isActive = group === "organization" ? stageIndex === 1 || stageIndex === 2 : group === "market" ? stageIndex === 3 || stageIndex === 4 : false;
					stage.classList.toggle("is-active", isActive);
					stage.setAttribute("aria-pressed", String(active === stage));
				});
			});

			const approachSection = document.querySelector<HTMLElement>(".approach-list")?.closest<HTMLElement>("section");
			bindTapParity(approachSection ?? null, "[data-approach-step]", (active) => {
				if (!approachSection) return;
				togglePressed(Array.from(approachSection.querySelectorAll<HTMLElement>("[data-approach-step]")), active);
				approachSection.dataset.activeStep = active ? String(Array.from(approachSection.querySelectorAll("[data-approach-step]")).indexOf(active) + 1) : "";
			});

			const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)");
			if (!reducedMotion.matches) {
				const revealObserver = new IntersectionObserver((entries, observer) => {
					entries.forEach((entry) => {
						if (!entry.isIntersecting) return;
						(entry.target as HTMLElement).classList.remove("is-reveal-pending");
						observer.unobserve(entry.target);
					});
				}, { threshold: 0.12 });
				document.querySelectorAll<HTMLElement>("[data-reveal]").forEach((section) => {
					section.classList.add("is-reveal-pending");
					revealObserver.observe(section);
				});
			}

			const assessment = document.querySelector<HTMLElement>("#grip-assessment");

			if (assessment) {
				const pairedControls = Array.from(assessment.querySelectorAll<HTMLElement>("[data-section]"));
				let selectedSection: string | null = null;

				const setActiveSection = (section: string | null) => {
					assessment.dataset.activeSection = section ?? "";
					pairedControls.forEach((control) => {
						const isActive = control.dataset.section === section;
						control.classList.toggle("is-active", isActive);
						if (control.matches("button, [role='button']")) {
							control.setAttribute("aria-pressed", String(isActive));
						}
					});
				};

				pairedControls.forEach((control) => {
					const section = control.dataset.section;
					if (!section) return;

					control.addEventListener("pointerenter", (event) => {
						if ((event as PointerEvent).pointerType !== "touch") setActiveSection(section);
					});
					control.addEventListener("pointerleave", (event) => {
						if ((event as PointerEvent).pointerType !== "touch") setActiveSection(selectedSection);
					});
					control.addEventListener("focus", () => setActiveSection(section));
					control.addEventListener("blur", () => setActiveSection(selectedSection));
					control.addEventListener("click", (event) => {
						event.stopPropagation();
						selectedSection = event.detail === 0 ? section : selectedSection === section ? null : section;
						setActiveSection(selectedSection);
					});
					control.addEventListener("keydown", (event) => {
						if (control.matches("button")) return;
						if (event.key === " " || event.key === "Enter") {
							event.preventDefault();
							selectedSection = section;
							setActiveSection(section);
						}
					});
				});

				document.addEventListener("click", () => {
					selectedSection = null;
					setActiveSection(null);
				});
			}
	</script>
</SiteLayout>

<style>
	.button-row { display: flex; gap: 26px; align-items: center; flex-wrap: wrap; }
	.text-link { font-weight: var(--font-weight-bold); text-decoration: underline; text-decoration-color: var(--amber); text-decoration-thickness: 2px; text-underline-offset: 6px; }
	.text-link:hover { color: var(--amber-text); }

	.eyebrow { margin-bottom: 24px; color: var(--amber-text); font-size: var(--type-card); font-weight: var(--font-weight-heavy); letter-spacing: var(--tracking-caption); line-height: var(--leading-card); text-transform: uppercase; }
	.eyebrow span { margin-inline: 8px; color: var(--slate); }
	.eyebrow-light { color: #f5aa70; }
	.hero { padding: 96px 0 105px; }
	.hero-grid { display: grid; grid-template-columns: minmax(0, 1fr) 308px; gap: clamp(45px, 7vw, 110px); align-items: end; }
	.hero-intro { max-width: 760px; margin-bottom: 36px; color: var(--text-primary); font-size: var(--type-lead); line-height: var(--leading-lead); }
	.hero-aside { box-sizing: border-box; align-self: center; width: 308px; padding-left: 24px; border-left: 4px solid var(--amber); }
	.hero-boundary-control { position: relative; aspect-ratio: 3 / 4; cursor: pointer; outline: none; }
	.hero-boundary-control:focus-visible { outline: 3px solid var(--amber); outline-offset: 6px; }
	.hero-boundary-visual { position: absolute; inset: 0; display: block; width: 100%; height: 100%; object-fit: contain; }
	.hero-boundary-visual-hover { right: auto; display: none; width: calc(100% * 7 / 6); }
	.hero-boundary-control:hover .hero-boundary-visual-default, .hero-boundary-control:focus-visible .hero-boundary-visual-default, .hero-boundary-control.is-active .hero-boundary-visual-default { display: none; }
	.hero-boundary-control:hover .hero-boundary-visual-hover, .hero-boundary-control:focus-visible .hero-boundary-visual-hover, .hero-boundary-control.is-active .hero-boundary-visual-hover { display: block; }

	.section-heading { margin-bottom: 58px; }
	.split-heading { display: grid; grid-template-columns: minmax(0, 1.35fr) minmax(280px, 0.65fr); gap: clamp(45px, 8vw, 120px); align-items: end; }
	.split-heading h2 { max-width: 760px; }
	.lead-copy { color: var(--text-secondary); font-size: var(--type-lead); line-height: var(--leading-lead); }
	.narrow-heading { max-width: 820px; }

	.diagnostic-grid { display: grid; grid-template-columns: repeat(4, 1fr); margin: 0; padding: 0; border-top: 4px solid var(--charcoal); border-bottom: 1px solid var(--rule); list-style: none; }
	.diagnostic-grid li { min-height: 285px; padding: 28px 26px 30px 0; border-right: 1px solid var(--rule); transition: background-color 160ms ease-out, box-shadow 160ms ease-out; }
	.diagnostic-grid li:not(:first-child) { padding-left: 26px; }
	.diagnostic-grid li:last-child { border-right: 0; }
	.diagnostic-grid li:hover, .diagnostic-grid li:focus-visible, .diagnostic-grid li.is-active { background: var(--amber-wash); box-shadow: inset 0 3px 0 var(--amber); outline: none; }
	.diagnostic-grid:has(li:hover) + .closing-line, .diagnostic-grid:has(li:focus-visible) + .closing-line, .has-active-card .closing-line { border-left-width: 6px; }
	.diagnostic-number-row { display: flex; align-items: flex-start; justify-content: space-between; min-height: 74px; }
	.item-number, .approach-number { display: block; margin-bottom: 48px; color: var(--amber-text); font-size: var(--type-card); font-weight: var(--font-weight-heavy); letter-spacing: var(--tracking-caption); line-height: var(--leading-card); }
	.diagnostic-ticks { display: flex; gap: 4px; padding: 7px 12px 0 0; opacity: 0; transition: opacity 160ms ease-out; }
	.diagnostic-ticks i { display: block; width: 3px; height: 14px; background: var(--amber); }
	.diagnostic-grid li:hover .diagnostic-ticks, .diagnostic-grid li:focus-visible .diagnostic-ticks, .diagnostic-grid li.is-active .diagnostic-ticks { opacity: 1; }
	.diagnostic-grid p { color: var(--text-primary); font-size: var(--type-card); line-height: var(--leading-card); }
	.closing-line { max-width: 810px; margin: 40px 0 0 auto; padding-left: 25px; border-left: 3px solid var(--amber); font-size: var(--type-lead); line-height: var(--leading-lead); }

	.grip-section { padding: 110px 0; background: var(--charcoal); color: var(--paper); }
	.grip-grid { display: grid; grid-template-columns: minmax(0, 1.1fr) minmax(370px, 0.9fr); gap: clamp(60px, 9vw, 140px); align-items: center; }
	.grip-copy h2 { max-width: 760px; }
	.grip-body { max-width: 730px; color: #c5cbc7; font-size: var(--type-body); line-height: var(--leading-body); column-count: 2; column-gap: 38px; }
	.grip-body p { break-inside: avoid; }
	.operating-line { margin-top: 40px; padding-top: 18px; border-top: 2px solid var(--amber); font-size: var(--type-lead); font-weight: var(--font-weight-bold); line-height: var(--leading-lead); }
	.grip-diagram { padding: 28px; border: 1px solid #53605a; background: #222b27; outline: none; }
	.grip-diagram:focus-visible { outline: 3px solid var(--amber); outline-offset: 4px; }
	.diagram-question { color: #f5aa70; font-size: var(--type-card); font-weight: var(--font-weight-heavy); letter-spacing: var(--tracking-caption); line-height: var(--leading-card); text-transform: uppercase; }
	.diagram-question-top { margin-bottom: 28px; }
	.diagram-question-bottom { margin: 28px 0 0; text-align: right; }
	.diagram-stage { min-height: 300px; display: grid; grid-template-columns: 1fr 70px 0.62fr; align-items: center; }
	.local-boundary { position: relative; aspect-ratio: 1; display: grid; place-items: center; padding: 58px 12px 16px; border: 2px solid var(--paper); box-sizing: border-box; }
	.diagram-label { position: absolute; top: 10px; left: 12px; right: 12px; color: #b9c0bc; font-size: var(--type-card); line-height: var(--leading-card); }
	.local-action { width: 42%; aspect-ratio: 1; align-self: end; display: grid; place-items: center; background: var(--amber); color: var(--charcoal); text-align: center; font-size: var(--type-card); font-weight: var(--font-weight-heavy); line-height: var(--leading-display); transition: transform 160ms ease-out; }
	.effect-line { display: flex; align-items: center; }
	.effect-line span { width: 20px; height: 2px; margin-right: 4px; background: #8a948f; }
	.effect-line span:last-child { background: var(--amber); }
	.effect-line::after { content: ""; width: 8px; height: 8px; margin-left: -5px; border-top: 2px solid #8a948f; border-right: 2px solid #8a948f; transform: rotate(45deg); }
	.downstream { display: grid; place-items: center; min-height: 92px; border: 1px dashed #89928e; color: #c5cbc7; text-align: center; font-size: var(--type-card); line-height: var(--leading-card); letter-spacing: var(--tracking-heading); transition: border-color 160ms ease-out, color 160ms ease-out; }
	.grip-diagram:hover .downstream, .grip-diagram:focus-visible .downstream, .grip-diagram.is-active .downstream { border-color: var(--amber); color: var(--amber); }
	.grip-diagram:hover .local-action, .grip-diagram:focus-visible .local-action, .grip-diagram.is-active .local-action { transform: translateY(-28%); }
	.grip-diagram:hover .effect-line span, .grip-diagram:focus-visible .effect-line span, .grip-diagram.is-active .effect-line span, .grip-diagram:hover .effect-line::after, .grip-diagram:focus-visible .effect-line::after, .grip-diagram.is-active .effect-line::after { border-color: var(--amber); background-color: var(--amber); }

	.loop-section { background: var(--reading); }
	.loop-flow { position: relative; display: grid; grid-template-columns: repeat(5, 1fr); margin: 0; padding: 0; border-top: 4px solid var(--charcoal); border-bottom: 1px solid var(--rule); list-style: none; }
	.loop-flow li { position: relative; min-height: 145px; padding: 24px 22px; border-right: 1px solid var(--rule); background: var(--reading); outline: none; transition: background-color 160ms ease-out; }
	.loop-flow li::after { content: ""; position: absolute; top: 50%; right: -6px; z-index: 2; width: 10px; height: 10px; border-top: 2px solid var(--charcoal); border-right: 2px solid var(--charcoal); background: transparent; transform: translateY(-50%) rotate(45deg); }
	.loop-flow li:last-child { border-right: 0; }
	.loop-flow li:last-child::after { display: none; }
	.loop-flow span { display: block; margin-bottom: 42px; color: var(--amber-text); font-size: var(--type-card); font-weight: var(--font-weight-heavy); letter-spacing: var(--tracking-caption); line-height: var(--leading-card); }
	.loop-flow .loop-group-badge { display: none; }
	.loop-flow strong { display: block; font-size: var(--type-card); line-height: var(--leading-card); }
	.loop-flow .signal-step { background: var(--amber-wash); }
	.loop-flow .signal-step::after { background: var(--amber-wash); }
	.loop-flow::before { position: absolute; z-index: 4; top: -39px; padding: 4px 8px; background: var(--reading); color: var(--slate); font-size: var(--type-card); font-weight: var(--font-weight-heavy); letter-spacing: var(--tracking-caption); line-height: var(--leading-card); text-transform: uppercase; }
	.loop-flow:has(li:nth-child(-n+3):hover)::before, .loop-flow:has(li:nth-child(-n+3):focus-visible)::before { content: "Organization"; left: 20%; }
	.loop-flow:has(li:nth-child(n+4):hover)::before, .loop-flow:has(li:nth-child(n+4):focus-visible)::before { content: "Market"; left: 60%; }
	.loop-flow:has(li:nth-child(-n+3):hover) li:nth-child(1), .loop-flow:has(li:nth-child(-n+3):focus-visible) li:nth-child(1), .loop-flow:has(li:nth-child(n+4):hover) li:nth-child(1), .loop-flow:has(li:nth-child(n+4):focus-visible) li:nth-child(1), .loop-section[data-active-group="organization"] li:nth-child(1), .loop-section[data-active-group="market"] li:nth-child(1) { background: var(--reading); }
	.loop-flow:has(li:nth-child(-n+3):hover) li:nth-child(2), .loop-flow:has(li:nth-child(-n+3):hover) li:nth-child(3), .loop-flow:has(li:nth-child(-n+3):focus-visible) li:nth-child(2), .loop-flow:has(li:nth-child(-n+3):focus-visible) li:nth-child(3), .loop-section[data-active-group="organization"] li:nth-child(2), .loop-section[data-active-group="organization"] li:nth-child(3), .loop-flow:has(li:nth-child(n+4):hover) li:nth-child(4), .loop-flow:has(li:nth-child(n+4):hover) li:nth-child(5), .loop-flow:has(li:nth-child(n+4):focus-visible) li:nth-child(4), .loop-flow:has(li:nth-child(n+4):focus-visible) li:nth-child(5), .loop-section[data-active-group="market"] li:nth-child(4), .loop-section[data-active-group="market"] li:nth-child(5) { background: var(--amber-wash); }
	.loop-section[data-active-group="organization"] .loop-flow::before { content: "Organization"; left: 20%; }
	.loop-section[data-active-group="market"] .loop-flow::before { content: "Market"; left: 60%; }
	.loop-flow li:focus-visible { box-shadow: inset 0 0 0 3px var(--amber); }
	.loop-note { position: relative; max-width: 890px; margin: 45px 0 0 auto; padding-top: 19px; color: var(--text-primary); font-size: var(--type-lead); line-height: var(--leading-lead); }
	.loop-note::before { content: ""; position: absolute; top: 0; left: 0; width: 62px; height: 3px; background: var(--amber); }

	.assessment-section { padding: 110px 0; background: #303936; color: var(--paper); }
	.assessment-grid { display: grid; grid-template-columns: minmax(320px, 0.7fr) minmax(560px, 1.3fr); gap: clamp(32px, 4vw, 64px); align-items: center; }
	.assessment-copy { min-width: 0; }
	.assessment-copy h2 { max-width: 470px; margin-bottom: 30px; font-size: clamp(2.6rem, 3.6vw, 3.6rem); }
	.assessment-intro { max-width: 430px; margin-bottom: 35px; color: #d4d9d6; font-size: var(--type-lead); line-height: var(--leading-lead); }
	.assessment-commitments { display: grid; gap: 12px; margin: 0 0 38px; padding-left: 24px; color: #f2eee7; font-size: var(--type-body); line-height: var(--leading-body); }
	.assessment-commitments li::marker { color: var(--amber); }
	.assessment-button { min-width: min(100%, 270px); }

	.score-card { min-width: 0; padding: clamp(28px, 4vw, 48px); border-radius: 16px; background: var(--reading); color: var(--charcoal); }
	.score-eyebrow { margin-bottom: 6px; color: var(--text-primary); font-size: clamp(1.25rem, 2.3vw, 1.85rem); font-weight: var(--font-weight-heavy); line-height: var(--leading-heading); letter-spacing: var(--tracking-heading); text-align: center; }
	.score-profile { position: relative; max-width: 610px; margin: 0 auto; }
	.score-ring { display: block; width: 100%; overflow: visible; }
	.score-segment { fill: none; stroke-width: 52; stroke-linecap: butt; cursor: pointer; transform-box: fill-box; transform-origin: center; transition: transform 250ms ease-out, opacity 250ms ease-out, filter 250ms ease-out; }
	.score-segment-strong { stroke: #172627; }
	.score-segment-weak { stroke: #d96b18; }
	.score-segment-moderate { stroke: #9a4328; }
	.score-segment.is-active { position: relative; z-index: 2; transform: scale(1.2); }
	.score-segment:focus-visible { outline: none; stroke-width: 60; filter: drop-shadow(0 0 2px #fcfaf6) drop-shadow(0 0 5px #d96b18); }
	.assessment-section[data-active-section]:not([data-active-section=""]) .score-segment:not(.is-active),
	.assessment-section[data-active-section]:not([data-active-section=""]) .score-result:not(.is-active) { opacity: 0.72; }
	.overall-score { position: absolute; top: 44%; left: 50%; display: flex; align-items: baseline; gap: 7px; margin: 0; transform: translate(-50%, -50%); }
	.overall-score strong { font-size: clamp(4.2rem, 8vw, 7rem); line-height: 0.8; letter-spacing: -0.07em; }
	.overall-score span { font-size: clamp(1.25rem, 2.4vw, 2rem); font-weight: var(--font-weight-heavy); }
	.hands-bar { position: relative; display: block; width: 100%; height: auto; margin-top: -28%; }

	.score-results { display: grid; grid-template-columns: repeat(5, minmax(0, 1fr)); margin-top: 8px; padding: 20px 0; border-top: 1px solid var(--rule); border-bottom: 1px solid var(--rule); }
	.score-result { position: relative; min-width: 0; min-height: 172px; display: flex; align-items: center; flex-direction: column; padding: 3px clamp(8px, 1.2vw, 16px); border: 0; border-right: 1px solid var(--rule); background: transparent; color: var(--charcoal); text-align: center; cursor: pointer; transform-origin: center; transition: transform 250ms ease-out, opacity 250ms ease-out, background-color 250ms ease-out; }
	.score-result:last-child { border-right: 0; }
	.score-result.is-active { z-index: 2; background: var(--reading); transform: scale(1.08); }
	.score-result:focus-visible { z-index: 3; outline: 3px solid var(--amber); outline-offset: 3px; }
	.score-icon { width: 42px; height: 42px; flex: 0 0 auto; margin-bottom: 10px; background: currentColor; mask: var(--score-icon) center / contain no-repeat; }
	.score-result-demand { --score-icon: url("/assets/operational-grip/icons/demand.svg"); }
	.score-result-ownership { --score-icon: url("/assets/operational-grip/icons/ownership.svg"); color: #b84f0c; }
	.score-result-results { --score-icon: url("/assets/operational-grip/icons/results.svg"); }
	.score-result-adaptation { --score-icon: url("/assets/operational-grip/icons/adaptation.svg"); }
	.score-result-ai { --score-icon: url("/assets/operational-grip/icons/ai.svg"); color: #7f351f; }
	.result-label { min-height: 3.45em; color: inherit; font-size: var(--type-card); font-weight: var(--font-weight-semibold); line-height: var(--leading-card); }
	.score-result strong { margin-top: auto; font-size: clamp(1.5rem, 2.5vw, 2rem); line-height: 1; }
	.result-strength { margin-top: 7px; color: var(--text-secondary); font-size: var(--type-card); font-weight: var(--font-weight-heavy); letter-spacing: var(--tracking-caption); line-height: var(--leading-card); text-transform: uppercase; }
	.score-result-ownership .result-strength, .score-result-ai .result-strength { color: currentColor; }
	.mobile-grip-summary { display: none; }
	.overall-reading { padding-top: 22px; }
	.overall-reading h3 { margin-bottom: 6px; font-size: var(--type-card); letter-spacing: var(--tracking-nav); }
	.overall-reading p { margin-bottom: 0; color: var(--text-primary); font-size: var(--type-card); line-height: var(--leading-card); }

	.approach-section { background: var(--paper); }
	.approach-list { margin: 70px 0 0; padding: 0; list-style: none; border-top: 4px solid var(--charcoal); }
	.approach-list li { display: grid; grid-template-columns: 100px minmax(0, 1fr); padding: 30px 0; border-bottom: 1px solid var(--rule); }
	.approach-list li { position: relative; outline: none; }
	.approach-list li:hover, .approach-list li:focus-visible, .approach-list li.is-active { background: var(--amber-wash); }
	.approach-list li > div { display: grid; grid-template-columns: 0.65fr 1fr; gap: 80px; align-items: baseline; }
	.approach-list h3, .approach-list p { margin-bottom: 0; }
	.approach-list p { color: var(--text-primary); font-size: var(--type-card); line-height: var(--leading-card); }
	.approach-number { margin: 3px 0 0; }
	.standard-quote { max-width: 940px; margin: 60px 0 0 auto; padding: 26px 0 0 36px; border-top: 4px solid var(--amber); border-left: 1px solid var(--rule); font-size: clamp(1.55rem, 3vw, 2.65rem); font-weight: var(--font-weight-bold); line-height: 1.14; letter-spacing: var(--tracking-display); }
	.approach-one { transition: color 160ms ease-out; }
	.mobile-approach-one { display: none; }
	.approach-list:has(li:nth-child(1):hover) + .standard-quote .approach-one-1, .approach-list:has(li:nth-child(1):focus-visible) + .standard-quote .approach-one-1, .approach-list:has(li:nth-child(2):hover) + .standard-quote .approach-one-2, .approach-list:has(li:nth-child(2):focus-visible) + .standard-quote .approach-one-2, .approach-list:has(li:nth-child(3):hover) + .standard-quote .approach-one-3, .approach-list:has(li:nth-child(3):focus-visible) + .standard-quote .approach-one-3, .approach-list:has(li:nth-child(4):hover) + .standard-quote .approach-one-4, .approach-list:has(li:nth-child(4):focus-visible) + .standard-quote .approach-one-4 { color: #D96B18; }
	.approach-section[data-active-step="1"] .approach-one-1, .approach-section[data-active-step="2"] .approach-one-2, .approach-section[data-active-step="3"] .approach-one-3, .approach-section[data-active-step="4"] .approach-one-4 { color: #D96B18; }

	[data-reveal] { transition: opacity 280ms ease-out, transform 280ms ease-out; }
	.is-reveal-pending { opacity: 0; transform: translateY(12px); }

	.body-work-section { padding-bottom: 82px; }
	.body-work-grid { display: grid; grid-template-columns: 1fr 1fr; gap: clamp(60px, 9vw, 130px); }
	.body-work-lead > p:last-child { max-width: 650px; color: var(--text-primary); font-size: var(--type-body); line-height: var(--leading-body); }
	.body-work-links { border-top: 4px solid var(--charcoal); }
	.body-work-links > a { position: relative; display: block; padding: 27px 0 27px 58px; border-bottom: 1px solid var(--rule); text-decoration: none; }
	.body-work-links > a:hover h3 { color: var(--amber); }
	.body-work-links > a:focus-visible { outline: 3px solid var(--amber); outline-offset: 4px; }
	.body-work-links > a > span { position: absolute; left: 0; top: 31px; color: var(--amber-text); font-size: var(--type-card); font-weight: var(--font-weight-heavy); line-height: var(--leading-card); }
	.body-work-links h3 { margin-bottom: 7px; }
	.body-work-links p { margin-bottom: 0; color: var(--text-primary); font-size: var(--type-card); line-height: var(--leading-card); }
	.boundary-statement { margin-top: 75px; padding: 28px 0; border-top: 1px solid var(--rule); border-bottom: 1px solid var(--rule); color: var(--text-secondary); font-size: var(--type-body); font-weight: var(--font-weight-semibold); line-height: var(--leading-body); text-align: center; }

	.thinking-section { background: var(--reading); }
	.thinking-grid { position: relative; display: grid; grid-template-columns: 1fr 1fr; border-top: 4px solid var(--charcoal); border-bottom: 1px solid var(--rule); }
	.thinking-grid::before, .thinking-grid::after { content: ""; position: absolute; z-index: 1; top: -4px; width: 50%; height: 4px; background: var(--amber); opacity: 0; transition: opacity 160ms ease-out; pointer-events: none; }
	.thinking-grid::before { left: 0; }
	.thinking-grid::after { right: 0; }
	.thinking-grid:has(.paper-card:hover)::before, .thinking-grid:has(.paper-card-link:focus-visible)::before { opacity: 1; }
	.thinking-grid:has(.tales-card:hover)::after, .thinking-grid:has(.tales-card-link:focus-visible)::after { opacity: 1; }
	.thinking-grid article { min-height: 330px; padding: 28px; border-right: 1px solid var(--rule); }
	.thinking-grid article:last-child { border-right: 0; }
	.thinking-grid p { color: var(--slate); }
	.thinking-grid a { color: inherit; text-decoration-color: var(--amber); text-decoration-thickness: 2px; text-underline-offset: 5px; }
	.thinking-grid a:hover { color: var(--charcoal); text-decoration-thickness: 3px; }
	.thinking-grid .content-type a { color: var(--charcoal); }
	.thinking-grid a:focus-visible { outline: 3px solid var(--amber); outline-offset: 4px; }
	.content-type { color: var(--amber-text) !important; font-size: var(--type-card); font-weight: var(--font-weight-heavy); letter-spacing: var(--tracking-caption); line-height: var(--leading-card); text-transform: uppercase; }
	.tales-card { background: var(--amber-wash); }
	.paper-card { background: var(--paper); }
	.paper-card-link { display: block; min-height: 100%; color: inherit; text-decoration: none; }
	.paper-card-link:hover .paper-title { color: var(--charcoal); text-decoration: underline; text-decoration-color: var(--amber); text-decoration-thickness: 3px; text-underline-offset: 5px; }
	.paper-card-link:focus-visible { outline: 3px solid var(--amber); outline-offset: 4px; }
	.paper-title { margin-bottom: 16px; color: var(--charcoal); font-size: clamp(1.8rem, 2.5vw, 2.25rem); font-weight: var(--font-weight-heavy); }
	.paper-fragment { width: 220px; height: 112px; margin: 0 0 16px; overflow: hidden; border: 1px solid rgba(29, 36, 33, .24); background: var(--reading); }
	.paper-fragment :global(img) { display: block; width: 100%; height: 100%; object-fit: cover; }
	.paper-line { max-width: 370px; margin-bottom: 0; color: var(--charcoal) !important; font-size: var(--type-card); font-weight: var(--font-weight-semibold); line-height: var(--leading-card); }
	.tales-card-link { display: block; min-height: 100%; text-decoration: none; }
	.tales-card-link:hover .tales-title { color: var(--charcoal); text-decoration: underline; text-decoration-color: var(--amber); text-decoration-thickness: 3px; text-underline-offset: 5px; }
	.tales-title { margin-bottom: 16px; color: var(--charcoal); font-size: clamp(1.8rem, 2.5vw, 2.25rem); font-weight: var(--font-weight-heavy); }
	.tales-fragment { width: 160px; height: 104px; margin: 0 0 16px; overflow: hidden; border: 1px solid rgba(29, 36, 33, .24); background: var(--reading); }
	.tales-fragment :global(img) { display: block; width: 100%; height: 100%; object-fit: cover; object-position: 76% center; }
	.tales-line { max-width: 370px; margin-bottom: 13px; color: var(--charcoal) !important; font-size: var(--type-card); font-weight: var(--font-weight-semibold); line-height: var(--leading-card); }
	.tales-coming { margin-bottom: 0; color: var(--text-secondary) !important; font-size: var(--type-caption); font-weight: var(--font-weight-semibold); letter-spacing: var(--tracking-caption); text-transform: uppercase; }

	.contact-section { padding: 110px 0; background: var(--charcoal); color: var(--paper); border-bottom: 5px solid var(--amber); }
	.contact-grid { display: grid; grid-template-columns: 1.1fr 0.9fr; gap: clamp(60px, 9vw, 130px); align-items: start; }
	.contact-copy > p:first-child { margin-bottom: 32px; color: #d4d9d6; font-size: var(--type-body); line-height: var(--leading-body); }
	.contact-note { margin: 25px 0 0; color: #b9c0bc; font-size: var(--type-body); line-height: var(--leading-body); }

	@media (max-width: 1000px) {
		.hero-grid { grid-template-columns: 1fr; }
		.hero-aside { width: 288px; margin-inline: auto; }
		.split-heading, .grip-grid, .assessment-grid, .body-work-grid, .contact-grid { grid-template-columns: 1fr; }
		.split-heading { gap: 12px; }
		.split-heading .lead-copy { max-width: 720px; }
		.diagnostic-grid { grid-template-columns: repeat(2, 1fr); }
		.diagnostic-grid li:nth-child(2) { border-right: 0; }
		.diagnostic-grid li:nth-child(-n+2) { border-bottom: 1px solid var(--rule); }
		.assessment-copy { max-width: 680px; }
		.score-card { width: 100%; max-width: 800px; margin-inline: auto; }
		.body-work-grid { gap: 55px; }
		.thinking-grid article:last-child { border-right: 0; }
	}

	@media (max-width: 720px) {
		.hero { padding: 72px 0 80px; }
		.hero-aside { width: 288px; }
		.button-row { align-items: center; flex-direction: column; gap: 22px; text-align: center; }
		.contact-copy > .button { display: table; margin-inline: auto; }
		.diagnostic-grid { grid-template-columns: 1fr; }
		.diagnostic-grid li, .diagnostic-grid li:not(:first-child) { min-height: auto; padding: 25px 0; border-right: 0; border-bottom: 1px solid var(--rule); }
		.diagnostic-number-row { min-height: auto; }
		.item-number { margin-bottom: 25px; }
		.closing-line { margin-top: 35px; }
		.grip-section { padding: 78px 0; }
		.grip-body { column-count: 1; }
		.diagram-stage { min-height: 250px; grid-template-columns: 1fr 45px 0.7fr; }
		.grip-diagram { padding: 20px; }
		.loop-flow { grid-template-columns: 1fr; border-bottom: 0; }
		.loop-flow li { display: grid; grid-template-columns: 55px 1fr; min-height: 82px; align-items: center; padding: 16px 20px; border-right: 0; border-bottom: 1px solid var(--rule); }
		.loop-flow span { margin-bottom: 0; }
		.loop-flow .loop-group-badge { grid-column: 1 / -1; width: max-content; margin: 0 0 10px; padding: 4px 8px; background: var(--reading); color: var(--slate); font-size: var(--type-card); font-weight: var(--font-weight-heavy); letter-spacing: var(--tracking-caption); line-height: var(--leading-card); text-transform: uppercase; }
		.loop-section[data-active-group="organization"] .loop-group-badge-organization,
		.loop-section[data-active-group="market"] .loop-group-badge-market { display: block; }
		.loop-flow li::after { top: auto; right: auto; bottom: -6px; left: 27px; transform: rotate(135deg); }
		.loop-flow::before { content: none !important; }
		.assessment-section { padding: 78px 0; }
		.assessment-grid { gap: 48px; }
		.assessment-copy { text-align: center; }
		.assessment-copy h2 { font-size: clamp(2.5rem, 11vw, 3.5rem); }
		.assessment-intro { margin-inline: auto; }
		.assessment-commitments { width: max-content; max-width: 100%; margin-inline: auto; text-align: left; }
		.assessment-button { display: flex; width: fit-content; margin-inline: auto; }
		.score-card { padding: 26px 20px; border-radius: 12px; }
		.score-results { grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 1px; padding: 1px; border: 1px solid var(--rule); background: var(--rule); }
		.score-result { min-height: 158px; padding: 16px 12px; border-right: 0; background: var(--reading); }
		.score-result.is-active { min-height: 176px; transform: none; }
		.score-result.is-active > strong { display: none; }
		.score-result.is-active .mobile-grip-summary { display: flex; align-items: center; justify-content: center; gap: 10px; margin-top: 12px; color: var(--amber-text); font-size: var(--type-card); font-weight: var(--font-weight-heavy); line-height: 1; }
		.mobile-grip-summary img { display: block; width: 74px; height: 28px; object-fit: cover; object-position: center; }
		.score-result:last-child { grid-column: 1 / -1; min-height: 142px; }
		.result-label { min-height: auto; }
		.score-result strong { margin-top: 14px; }
		.overall-reading { padding-top: 24px; }
		.approach-list li { grid-template-columns: 55px 1fr; }
		.approach-list li > div { grid-template-columns: 1fr; gap: 8px; }
		.mobile-approach-one { display: none; position: absolute; right: 20px; bottom: 16px; color: #D96B18; font-size: var(--type-subheading); font-weight: var(--font-weight-heavy); line-height: var(--leading-heading); }
		.approach-list li.is-active { padding-bottom: 58px; }
		.approach-list li.is-active .mobile-approach-one { display: block; }
		.standard-quote { padding-left: 20px; }
		.thinking-grid { grid-template-columns: 1fr; }
		.thinking-grid article { min-height: auto; border-right: 0; border-bottom: 1px solid var(--rule); }
		.thinking-grid article:last-child { grid-column: auto; border-top: 0; }
	}

	@media (max-width: 440px) {
		.diagram-stage { grid-template-columns: minmax(0, 1.35fr) 18px minmax(0, 0.92fr); }
		.diagram-label { line-height: var(--leading-heading); }
		.effect-line span { width: 4px; margin-right: 2px; }
		.score-card { padding-inline: 16px; }
		.score-ring { width: calc(100% + 8px); margin-left: -4px; }
		.overall-score { top: 45%; }
		.hands-bar { width: calc(100% + 8px); margin-left: -4px; }
		.score-results { grid-template-columns: 1fr; }
		.score-result:last-child { grid-column: auto; }
		.score-result { min-height: 136px; }
	}

	@media (prefers-reduced-motion: reduce) {
		.score-segment, .score-result, .diagnostic-grid li, .local-action, .downstream, .loop-flow li { transition: none; }
		.approach-one { transition: none; }
		.score-segment.is-active, .score-result.is-active { transform: none; }
		.grip-diagram:hover .local-action, .grip-diagram:focus-visible .local-action { transform: none; }
		.is-reveal-pending { opacity: 1; transform: none; transition: none; }
	}
</style>
