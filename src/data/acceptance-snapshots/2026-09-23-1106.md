| Item | Verifier | Acceptance condition | Test |
| --- | --- | --- | --- |
| 001 | Independent AI Reviewer | `accept-closeout` validates and appends `release-prepared` when missing followed by `accepted`, then persists the lifecycle file once and leaves the suite immutable. | Inspect the helper and CLI, run the lifecycle tests, and exercise both missing and existing release-prepared cases. |
| 002 | Independent AI Reviewer | Optional `standing-plus` mappings accept only unique increment ids 001–010 that exist in the suite and map to existing live standing ids. They are stored only on the accepted event and do not edit the checklist or `LIVE_ITEMS`. | Inspect validation boundaries and run valid, malformed, duplicate, unknown-increment, and unknown-standing cases. |
| 003 | Independent AI Reviewer | Build housekeeping fails before Astro diagnostics when the standing checklist contains ids absent from `LIVE_ITEMS`, `PROCESS_ITEMS`, and `RETIRED_ITEMS`, naming every missing id in checklist order. | Run the housekeeping fixture containing 836 and 837, then run the current checklist through the same assertion. |
| 004 | Independent AI Reviewer | Standing items 831–835 retain Douglas's accepted Pass verdicts from suite 2026-09-22-2028, the active count is 97, and the accepted suite remains unchanged. | Compare the standing ledger with the accepted suite mappings, run acceptance-status tests, and inspect the frozen-suite diff. |
| 005 | Independent AI Reviewer | The residual changes do not alter homepage or other public-site content, staff-document source wiring, routes, sitemap behavior, or indexing controls, and the test build succeeds. | Inspect the final diff and built routes, then run `pnpm build:test`. |
