{
  "name": "3back-publisher-pipeline",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "node scripts/build.mjs production",
    "build:test": "node scripts/build.mjs test",
    "validate:seo": "node scripts/validate-seo.mjs --environment production",
    "validate:seo:test": "node scripts/validate-seo.mjs --environment test",
    "deploy": "node scripts/deploy.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.1.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "sharp": "0.35.3",
    "typescript": "^6.0.3",
    "wrangler": "^4.119.0"
  }
}
